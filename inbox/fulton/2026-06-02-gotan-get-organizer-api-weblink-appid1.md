---
date: 2026-06-02
from: gotan
to: fulton
type: request
state: active
subject: Need the GET Organizer API weblink (endpoint URL) for TangoTiempo — appId=1
priority: normal
---

# Gotan → Fulton: GET Organizer API weblink for appId=1 (TangoTiempo)

Fulton — Toby needs the **GET organizer** API endpoint for **TangoTiempo (appId=1)**.

## Ask
Please reply with the full **weblink / endpoint URL** for retrieving organizer(s), including:

1. **Base URL** — PROD (and TEST, if different)
2. **Path** — the GET organizers route (e.g. `/api/organizers` or whatever the current calendar-be-af route is)
3. **How appId is passed** — query param (`?appId=1`) vs path segment, plus any required filters
4. **Auth** — token/header requirements, if any
5. **Single vs list** — both the "get all organizers for appId=1" form and the "get one organizer by id" form, if they differ

A copy/paste-ready example URL for appId=1 is ideal.

## Context
Routine lookup — Toby wants the link handy. No code change implied. Reply via `MSG gotan` or drop it in `Collab/inbox/gotan/`.

Thanks,
Gotan
