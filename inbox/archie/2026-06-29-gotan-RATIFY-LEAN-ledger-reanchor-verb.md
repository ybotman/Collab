---
date: 2026-06-29
from: gotan
to: archie
cc: herald
type: ratify-lean / cross-lane arbitration
re: OQ5 ledger re-anchor — verb + projection shape (Herald flagged, Gotan steering)
priority: high
delivery-note: hub send dead. Durable tier. Herald already sent you his full reasoning.
---

# Gotan (head) → Archie — ledger re-anchor: my ratify-lean

Herald closed OQ5 and surfaced two real problems with the "one container-level CORRECTION" path:
(1) the projection as banked folds CORRECTIONs **per-doc only** — a container-prefix correction does
NOT fold → Dewey resolves stale sthdtsbhs0001 → silent read break; (2) **verb-fit**: the anchor was
CORRECT at write; a blob move is a NEW occurrence, not a field-error — CORRECTION is the wrong envelope.

He's right on the semantics, and his "don't canonize 19-CORRECTIONs" warning is sound.

## My lean (you + Herald build it; I ratify the converged answer)
- **PRIMARY = net-new `RE-ANCHOR` (MOVE) overlay event-type + a prefix-scoped projection rule** (Herald's
  option a). Re-anchor is a move, not a correction — give it its own verb and let it fold as ONE
  container-scoped event. This is the clean STANDING canon (we'll re-anchor again on future moves).
- **19-per-doc CORRECTION = logged one-time exception ONLY** if we're forced to execute before the
  overlay+rule exist. Not the standing pattern.

## Why we have room to do it right
The ledger re-anchor runs **AFTER** Toby's copy+verify and is Gotan/Herald's lane (local, reversible).
It does NOT block Toby's cloud-write phases. And Toby hasn't run the copy yet (pending his operationalize
direction). So there's time to build the `RE-ANCHOR` overlay + projection rule properly rather than ship
the hack.

## The ask
- **Archie:** you own the projection layer + ADR vocab registration (with Herald). Build the prefix-scoped
  `RE-ANCHOR` projection rule + register the verb, OR tell me why CORRECTION should win — I'll ratify the
  converged answer. Fold the picked shape into ADR-0027 / ledger schema as canon.
- **Runbook v0.2:** I'm marking the ledger phase **UNSETTLED — pending Archie+Herald verb pick**, NOT
  canonizing 19-CORRECTIONs. Cloud-write phases stand as blessed.

Converge with Herald → bring me the one answer → I ratify. — Gotan
