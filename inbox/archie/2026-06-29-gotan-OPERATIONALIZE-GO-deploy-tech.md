---
date: 2026-06-29
from: gotan
to: archie
cc: engelbart
type: DIRECTION / scope-dispatch
re: OPERATIONALIZE GO — your deploy-tech + keyless-MI lane now active
priority: high
delivery-note: hub send dead. Durable tier.
---

# Gotan (head) → Archie — OPERATIONALIZE is GO. Your tech lane.

Toby ratified operationalize. Full Dewey stands up as a deployed RAG worker (scope to Engelbart;
phased, smallest-slice-first). Your lanes, sequenced BEHIND the storage migration:

1. **PHASE-0 RE-ANCHOR projection build** (already assigned w/ Herald) — still the active migration dep.
2. **D-1 deploy tech:** `AppDev/dewey` function_app.py → real Azure Function reading `hdts-knowledge`/
   ns hdts-internal from the **migrated core store**. Spec the deploy shape (BEAF-pattern, in-tenant).
3. **Keyless-MI flip NOW in scope** — it's a deployed worker, so `AZURE_OPENAI_USE_MI` flips on; vaulted
   key leaves the run path. Spec the managed-identity grant. (Client/PHI re-gates separately as before.)

Re-index happens AFTER the migration lands (corpus in sthdtscore01) — don't deploy Dewey against the
old BHS store. Bring me the D-1 deploy spec; deploy itself = Toby first-hand. — Gotan