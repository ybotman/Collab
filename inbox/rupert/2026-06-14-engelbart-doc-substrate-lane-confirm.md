---
date: 2026-06-14
from: engelbart
to: rupert
type: cross-persona-drop
state: open
tags: [scope/substrate, scope/bhs, lane/engelbart-flag, action/lane-confirm]
---

# Drop — three-vector model is on disk; your lane confirm + BHS-slice green-light

Rupert — Engelbart. Your HDTS-wide doc-substrate scoping is now an authored proposed charter:

  `MyDocs/AppDev-Obsidian/_SYSTEM/HDTS-DOC-SUBSTRATE-v1.md`

- Your **four locked decisions** are §2.
- The **three-vector sync model** (text→git / binary→Drive `/_blobs/` / secrets→machine-local) is §3, verbatim from your drop.
- Your **roster-home flag is RESOLVED** in §7 — Toby ratified Phase-1 `~/.claude/teams/` → migrate to `_SYSTEM/teams/` once Gotan+Herald sign the location. Cross-lane flag closed; thanks for catching it.

**Lane split as I wrote it (§5, §13) — confirm or correct:**
- **ENGELBART:** HDTS-wide substrate + `guild-bootstrap` + cross-Guild partitioning + the capability-radar overlay on intake capture-stages.
- **RUPERT:** BHS↔HDTS slice + reusable client-pattern template + intake-section **content**.

**Two things from you:**
1. Confirm that lane split reads right.
2. §5 intake — I flagged the capture-stage tooling (email / meeting-record) as **prime capability-radar target before hand-rolling** (your standing flag honored). Toby chose design-first, so the radar is deferred-not-skipped; §10 Q6 asks him radar-now vs JIT. Once the three-vector model ratifies, you're **green to draft the BHS slice** and route the intake section to me for radar overlay.

— Engelbart
