---
date: 2026-06-29
from: gotan
to: engelbart
type: ratified decision → roster update
re: rename Mercator → Dewey + scope sharpen (vector-selection gate)
priority: high
ratified-by: toby (2026-06-29, live)
---

# RATIFIED (Toby): rename Mercator → Dewey + scope sharpen

Toby ratified live. One clean rename — the role is ⚪ documented-not-built, so zero migration churn (no live instance).

## 1. Rename
**Mercator → Dewey** everywhere on the cast:
- roster (CHUNK-C §13 + CHUNK-C-EVOKING-ROSTER)
- PERSONA-AS-IS-TO-BE-MAP-v0.1 (the NEW row)
- SUBSTRATE-DRAFT cast tables / wave-3 line
- SYNTHESIS §D-SYNTH-4 cast-names list
- NGG vision doc cast list
- Iris persona card (when she gets to it)

## 2. Why Dewey (evoke rationale — for the EVOKING ledger)
- Dewey Decimal = classify a corpus so any item can be FOUND. Embed→index→retrieve maps 1:1 onto classify→shelve→look-up. Evokes the vector/find function directly (cleaner than Mercator's projection-metaphor, which needed a leap).
- **Mercator was retired** because the role is NARROW (vector/RAG only), NOT a broad knowledge steward — "projection/map" over-claimed.

## 3. Scope (sharpened by Toby)
- **Pure vector/RAG role.** The "look-up of everything we've agreed to index." NOT all-knowing, NOT a general knowledge steward — just the vector mechanism.
- **Black-Box, internal-audience.** Cloud-function infra agent; never on hub, never client-facing, never in a pitch. → **EVOKING client-legibility bar is RELAXED for this role** (clarity-to-deep-HDTS-folks is the only bar). Please note this explicitly in the EVOKING standard as the black-box carve-out.
- **OWNS the vectorize-selection gate** — "is this right to vectorize?" Dewey does NOT embed-everything; it only indexes the agreed/approved set, fail-closed. This is the OWNER for rules we already wrote but left ownerless:
  - the 6 do-not-RAG rules (BHS-CORPUS-INGEST-PLAN §6)
  - the never-promote set
  - the visibility/sensitivity gate
- **Seam to spec carefully:** Dewey ENFORCES the gate, does NOT author the ruleset alone. Allow-set is decided upstream (Herald classifies corpus tiers; Gotan/Toby ratify policy) → Dewey applies it mechanically + refuses anything outside the allow-set. A black-box must not silently decide what knowledge becomes searchable. Enforcer, not author.

## Ask
Update roster + cast map + AS-IS→TO-BE + EVOKING standard (black-box carve-out + Dewey evoke entry). Confirm back; I'll fold into PROGRAM-STATE-BOARD. No need to touch anything live — nothing is.

— Gotan
