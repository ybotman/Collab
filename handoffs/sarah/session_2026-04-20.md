---
date: 2026-04-20
persona: sarah
type: handoff
state: active
feature: [chrome-refresh, beginner-mode, explore-mode, organizer-mode, messaging-system, city-country-discipline]
keywords: [tiempo-408, tiempo-409, tiempo-410, tiempo-411, tiempo-412, tiempo-413, 2.1.0-candidate, no-fallback-rule, test-bundle]
appid: 1
audience: all
permanence: milestone
---
# Session Handoff: Sarah @ 2026-04-20 (full day through ~16:30Z)

## 💡 Status & Blocker

- **One-liner:** TT 2.1.0 release candidate fully on TEST — six tickets bundled (TIEMPO-408/409/410/411/412/413). Awaiting Toby verification + DEPLOY-PROD cue. No blockers on my side.
- **Blockers:** None.

## 🔨 Execution Log

### Shipped to TEST this session (all land as one 2.1.0 PROD release per Toby single-release directive)

- **TIEMPO-410** — Explore map polish: map stays mounted when filter returns zero events; mobile marker/popup/legend sizing fixes. `src/app/components/Explore/ExploreMap.js`. PRs #242 → #243 merged 2026-04-19 17:17Z.
- **TIEMPO-411** — Killed auto-trigger browser geolocation in MapCenterModal + WelcomeModal. iOS Safari was rejecting setTimeout-delayed getCurrentPosition as non-gesture → "not allowed" banner even when OS permission granted. Users now pick explicitly via map crosshair or user-tap "Use My Location". Dead toast/Snackbar UI deleted. `src/app/components/Modals/misc/MapCenterModal.js` + `src/app/components/Modals/Welcome/WelcomeModal.js`. PRs #244 → #245 merged 2026-04-19 17:23Z.
- **TIEMPO-412** — Wired orphaned ComposeMessageModal (TIEMPO-387 finish): added "Compose Message" sidebar item for SA/SO users. Unblocks Toby sending TEST NU + RO welcome messages for the BEG+EXPLORE launch. `src/app/components/UI/SidebarDrawer.js`. PRs #246 → #247 merged 2026-04-19 22:01-02Z.
- **TIEMPO-413** — New `/organizer` route + 4-tab reorder. Tabs now LOCAL → ORG → EXPLORE → BEGINNER per Toby's 4-persona model. `/organizer` is mapcenter-scoped, 90-day window, events grouped by `ownerOrganizerName` in MUI Accordions collapsed by default, sorted by event count descending, count chip in group header. New files: `src/app/organizer/page.js`, `src/app/components/Organizer/OrganizerGroupedList.js`. Edits: `ModeToggle.js` (4-wide, widened container, added `organizer` option + purple color), `useMode.js` (path-first derivation + setMode routing). PRs #248 → #249 merged 2026-04-20 15:59Z.
- **TIEMPO-413 refinement** — Per Toby: (1) show all 3 categories (`categoryFirst`/`Second`/`Third`) color-coded via broader local-category palette (Milonga red, Practica teal, Class blue, etc.); (2) venue name chip replaces mastered-city chip (prefer `venueAbbr`, fallback `venueName` with 160px maxWidth + ellipsis); (3) right-side distinct beginner pills — `For Beg` (filled green + School icon, square-rounded) and `Beg-friendly` (outlined muted + WavingHand icon). PR #251 sandbox→DEVL merged, PR #252 DEVL→TEST hit add/add squash-SHA conflict, PR #253 cherry-pick → TEST merged 2026-04-20 16:22Z.

### Strategic / architectural

- **Signed off on `CITY-COUNTRY-DATA-DISCIPLINE-REVIEW.md`** (Toby-greenlit). TT-FE lane conclusion locked: historical discipline clean (446/446 legacy venues filled); current 1,011 null-city gap is 100% Porter/AI-discovery lane, not TT-FE origin. My `VenueModalAddWithSearch` search-mode hole framed as T3-defensive-only (Fulton's `Venues_AutoMaster` Phase 2 makes it redundant).
- **CALBEAF-130 pushback accepted by Quinn.** 6 address-fragment `venue.country` values were initially routed to me as FE-create-path bug; evidence (5-minute 2026-04-06 batch, column-shift pattern, FE has no `country` write path) proved it's a Porter/AI-discovery ingest-parser bug. Re-routed to AIDI (root cause) + Fulton (data patch). I'm off the ticket; AIDI will ping for post-patch re-probe.
- **Captured HARD RULE in memory** (`memory/feedback_no_location_fallback.md`): no fallback for mastered* location fields. When `masteredCityId/Name`, `masteredCountryId/Name`, `masteredDivisionId/Name`, `masteredRegionId/Name` are null, render NOTHING and write NOTHING — never substitute from `venue.country` free-text (unvalidated, demonstrably wrong), geocoder output, nearest-city guess, or defaults like "US"/"Unknown". Reason: Estonia venue had `country: "US"` pre-Track A; any fallback re-introduces `/boston`-pollution pattern the doc was built to prevent. Retracted my own suggestion #2 from the 20:34Z Budapest analysis reply. Applies forward to all mastered/identity fields by default.
- **Captured architectural fact in memory** (`memory/project_mastered_cities_shared.md`): mastering collections (`masteredcities`, `mastereddivisions`, `masteredregions`, `masteredcountries`) are a single SHARED GLOBAL POOL across all appIds. Only venue/event/organizer docs are appId-scoped. I had mis-framed HJ as "needs its own pool"; Toby corrected.
- **Ratified 4-tab architecture (LOC / ORG / EXPLORE / BEG).** Original proposal was unified single-pane view with `?view=` param; I pushed back in TIEMPO-408 and shipped 3 separate routes. Toby's new 4-persona model (regular local, organizer-watcher, traveler, beginner) fits the separate-route architecture cleanly — each tab answers a distinct question (when/who/far/safe). Future: `feature: favorited-organizers` will let pinned orgs float to top of `/organizer` list.

### Files modified/added this session

**Added:**
- `src/app/organizer/page.js`
- `src/app/components/Organizer/OrganizerGroupedList.js`
- `~/.claude/projects/.../memory/feedback_no_location_fallback.md`
- `~/.claude/projects/.../memory/project_mastered_cities_shared.md`

**Edited:**
- `src/app/components/Explore/ExploreMap.js`
- `src/app/components/Modals/misc/MapCenterModal.js`
- `src/app/components/Modals/Welcome/WelcomeModal.js`
- `src/app/components/UI/SidebarDrawer.js`
- `src/app/components/UI/ModeToggle.js`
- `src/app/hooks/useMode.js`

## ⏭️ Next Step Handshake

1. **Toby:** verify TIEMPO-410/411/412/413 on test.tangotiempo.com, send the TIEMPO-412 NU + RO welcome messages via the new SA/SO sidebar "Compose Message" item.
2. **Toby:** greenlight DEPLOY-PROD when satisfied. On his cue, I'll bump `package.json` to `2.1.0`, follow PROD-DEPLOY-PROTECTION protocol, promote TEST → PROD as a single bundle.
3. **Me (next session start):** run INBOX2, check Fulton's `Venues_AutoMaster` Phase 1 batch completion status, check AIDI for post-patch CALBEAF-130 re-probe ping, check Quinn for Track B 50-row row-list ping.
4. **Pending greenlight (not started):** event-create compliance cleanup — `CreateEventDetailModal.js:778` hardcoded `'Default Region'` fallback (direct no-fallback-rule violation) + four `|| selectedRegion` fallbacks in `useEvents.js`. I sized this earlier; waiting on Toby's decision.

## 🗂️ Context / pointers for next me

- **Repo:** `/Users/tobybalsley/MyDocs/AppDev/MasterCalendar/tangotiempo.com`
- **Live TEST:** https://test.tangotiempo.com (v2.1.0 candidate, all six tickets landed)
- **Live PROD:** https://tangotiempo.com (still v1.22.24 — TIEMPO-407 hotfix only, untouched since)
- **Data discipline doc:** `MasterCalendar/docs/CITY-COUNTRY-DATA-DISCIPLINE-REVIEW.md`
- **Current branch:** `sandbox/2026-04-17-TIEMPO-408-chrome-beginner-p51` (clean, all work merged through to TEST)
- **Squash-merge SHA divergence gotcha:** today's PR #252 hit add/add conflict going DEVL→TEST because squash merges create distinct-SHA commits that differ between DEVL and TEST even when content is logically identical. Workaround: cherry-pick the DEVL squash onto a fresh branch off TEST, PR that. Logged for future-me.

## 📐 Standing rules carry (unchanged this session)

- PROD stay-out absolute until Toby's explicit "DEPLOY-PROD sarah 2.1.0"
- Hub-first messaging (`mcp__persona__send`), CollabMsg fallback
- Auto-mode active for low-risk work; confirm for risky/irreversible
- **No fallback on mastered/location fields — ever** (new hard rule this session)
- Mastered collections are shared global pool, not appId-partitioned
