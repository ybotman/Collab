# Session Handoff: Sarah @ 2026-03-24 ~01:00

## Current Status
**Two tracks: TIEMPO-391 (Spotlight) paused, Geo API 429 fix in progress on DEVL**

---

## Track 1: TIEMPO-391 Spotlight Self-Service (PAUSED)

### Branch
`feature/TIEMPO-391-spotlight-self-service` (4 commits, not pushed)

### Docs Created
- `docs/TIEMPO-391-SPOTLIGHT-SELF-SERVICE-DESIGN.md` — Full design doc
- `docs/TIEMPO-391-TEST-PLAN.md` — TEST → PROD validation plan

### Design Decisions (Approved)
- Recurring: this date only (default)
- Name: profile displayName, allow edit
- Auto-exit: on navigation
- Phase 1: minimal (toggle + modal only)

### Next Steps
1. BUILDER: Create 4 components (SpotlightModeContext, Toggle, Modal, Hook)
2. Modify: useCalendarPage.js, SidebarDrawer.js, Providers.js
3. Coordinate with Fulton on BE PROD deployment

---

## Track 2: Geo API 429 Errors (ACTIVE - DEVL)

### Problem
- **835 x 429** errors on Geo_GoogleGeolocate in 7 days
- **Rapid-fire bursts** from same locations (Washington 25 calls/30sec, Bedford 8 calls/1sec)
- **Aberdeen, Hong Kong** polling every 3-10 min (bot or heavy user)

### Root Cause
Multiple parallel calls to Google Geo:
```
Page Load
├─► calendar/layout.js
│   ├─► getGeolocationData()      → Google Geo
│   └─► fetchAllGeolocationData() → Google Geo (again!)
└─► AuthContext.js (on login)
    ├─► getGeolocationData()      → Google Geo
    └─► fetchAllGeolocationData() → Google Geo (again!)
```

### Fixes Applied (DEVL, not pushed)
1. `geolocationHelper.js` — SessionStorage cache (5 min) + rate limit tracking
2. `trackingHelper.js` — SessionStorage cache + rate limit tracking + timeout 2s→3s

### Commits on DEVL (not pushed)
- `3b738f40` fix: Add caching and rate-limit tracking to Google Geo API
- `eaa634a8` fix: Add sessionStorage caching to trackingHelper.js

### Still TODO
- **Consolidate call paths** — Single Google Geo call per page load
- **MSG to Fulton** — Sent asking about BE-side caching (pushed to Collab)

---

## Git State

| Branch | Status |
|--------|--------|
| `DEVL` | +2 commits (geo fixes), NOT PUSHED |
| `feature/TIEMPO-391-*` | +4 commits (design/test plan), NOT PUSHED |

---

## Next Session Should
1. Run INBOX
2. **Push DEVL** with geo fixes
3. **Consolidate Google Geo calls** (single call path)
4. Check Fulton's response re: BE caching
5. Resume TIEMPO-391 BUILDER when geo issue stabilized

---

## Key Context
- 7-day API error report from Azure App Insights
- Rapid-fire pattern = code bug (multiple parallel calls)
- Aberdeen HK = bot or heavy user
- Caching fix helps but doesn't solve root cause (duplicate calls)
