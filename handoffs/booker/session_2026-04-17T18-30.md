# Session Handoff: Booker @ 2026-04-17 18:30 UTC

## Current Status

FB account soft-blocked ("You're Temporarily Blocked" on events feature). Cooldown armed in `data/rate-limit-state.json` until 2026-04-19 ~12:57 local. Pipeline will refuse to run via preflight gate unless `--force` is used. Safety rails (B1 + B2) wired into `batch-pipeline.ts`; watchdog path rot fixed. 6 bad rows identified in DQ sweep, CSV handed to Porter. No code running, account in cooldown.

## What I Did This Session

- **2026-04-17 rolecall → workshop-refinement decisions (x4, all ratified)** — Quinn/AIDI coordination on category ladder + skip_class_only. My side: keep duration tie-breaker, drop skip_class_only narrow sense, ready to implement when CALBEAF ticket drops.
- **AUTONOMY-DEPLOYMENT-PLAN review** — AIDI drafted v1→v2 with my Booker-side input (gaps B1-B9, telemetry shape, Telegram triggers). All deltas accepted.
- **Niche-readiness dossier** (Gotan) — 150-word inventory of what's tango-hardcoded vs niche-agnostic in Booker's FB parser. A2 port estimate 2-3 days config + 1 week classifier.
- **FB SOFT-BLOCK INCIDENT (2026-04-17)** — overnight run hit 339 groups in 5h15m (~65/hr), exceeded FB-AWARE target by 10x and CLAUDE.md budget by 100x. Account soft-blocked on events feature. Chrome segfaulted at 11:19 local. Probable block trigger: ~00:30-01:00Z when events-per-group collapsed 85→24 while group-rate stayed 57-70/hr. **We continued clicking through for ~117 more group visits post-block** (no inline block-detector).
- **Safety rails built (B1 + B2 + watchdog-path)**:
  - Created `scripts/lib/safety.ts`: `detectBlockInDom`, `readCooldown`, `recordBlock`, `clearBlock`, `humanDelay`, `isInCooldown`
  - Patched `scripts/batch-pipeline.ts`: preflight cooldown check (exit=2 if blocked); post-Stage-1 DOM block-detector; replaced flat `--group-delay=10000` with jittered `humanDelay(min,max)` (defaults 5-30 min); `--max-groups-per-run=35` cap; added `--force` and `--skip-safety` CLI flags
  - Fixed `scripts/watchdog.sh` — Phase-2 rename path rot corrected; expanded regex to catch "temporarily blocked" / "misusing this feature"; reads `rate-limit-state.json`; threshold dropped 3→1
  - Armed `data/rate-limit-state.json` with real incident (48h cooldown). Verified pipeline refuses with exit=2.
- **DQ sweep (group-context leakage)** — 5 targeted SQLite queries, found 6 confirmed bad rows (4 patchable, 2 delete-only) + 59 national/international FB groups as leak exposure surface. Delivered CSV to Porter: `data/runs/prod_scrub_group_context_leak_2026-04-17.csv`. Full writeup: `data/runs/dq_sweep_group_context_leak_2026-04-17.md`. Porter ACK'd, building WARN-only DQ gate (venue-name country-marker check + state/country consistency), flagged P13 for AIDI.
- **Posting strategy** — advised on FB posting risks. 4 post-ready USA multi-day event templates provided (Denver Does Tango, ATUSA, Denver Close Encounters, Motown Marathon) with both FB event links and TT `/event/{id}` URLs.
- **Two memories saved:** `feedback_fb_access_conservative.md` (default NO on scrape runs, rails required, posting rule), `reference_jira_projects.md` (actual project keys — CALAI does not exist; discovery work has been filed under CALBEAF).

## Incident File

`data/runs/incident_2026-04-17_fb-soft-block.md` — full FTPNTD writeup including timeline, monitoring-layer diagnosis, cooldown protocol.

## Next Session Should

1. **DO NOT touch FB in booker-chrome profile.** Cooldown until 2026-04-19 ~12:57 local minimum. Benign probe (`/me`, not events, from different browser profile) to test clear.
2. **If probe clean at T+24h**: wait another 24h (T+48h total) before any events page load.
3. **If probe STILL blocked at T+24h**: extend cooldown another 24h, `recordBlock()` again with new TTL.
4. **Code work that can proceed now** (no FB needed):
   - Write the 3 CALAI-equivalent tickets in CALBEAF (since that's the convention): country-marker parser, group-context fallback removal, orphan-row city/state source from Harvey CSV
   - `config/facebook-groups.json` — add `scope: "local" | "national" | "international"` field, populate for the 59 groups Sweep 5 identified
   - Coordinate with Porter on timing of prod scrub (patch 3, delete 2)
5. **Before any scrape resume**: verify rails are green (`pgrep -f watchdog.sh`, rate-limit-state.json not blocked, `node scripts/batch-pipeline.ts --dry-run` exits 0 cleanly).
6. **First resume run**: `--max-groups-per-run=5`, `--min-group-break-ms=600000 --max-group-break-ms=1200000` (10-20 min jitter). Monitor overnight.

## Key Context

- **Rate was the trigger, not quantity or duration.** ~65/hr vs FB-AWARE target 2-5/hr was the overwhelming signal. Quantity ~240 before block was meaningful but not extreme; duration 3.5h was neutral.
- **FB Page posting is SAFE during cooldown** — different surface (Page entity, not personal nav-rate). Encouraged.
- **Posting FROM personal Booker account to groups = highest risk.** "Caught bot + now posting" reads as evasion to FB's ML. Forbidden until ≥2 weeks clean browsing post-cooldown.
- **59 FB groups are "national/international" scope.** Every event posted in these risks group-context leakage in current code. Booker-side fix is the only real closure; Porter WARN-only gate is the defensive second layer.
- **6 prod MongoDB rows are corrupted** (Lisbon, Montreal, Naples, Sacramento-stitched, Morbihan-stitched + 1 not-yet-synced Perth). Fix sequence: Booker code → re-sweep → Porter patch/delete from CSV.
- **Watchdog is fixed but NOT running as a service.** Manual `bash scripts/watchdog.sh &` before any scrape run until launchd/systemd wiring happens in AUTONOMY-DEPLOYMENT-PLAN phase 2.

## Hub / Coordination State

- Porter: ACK'd CSV, waiting to apply after Booker-side fixes land; building WARN-only DQ gate as their P13
- AIDI: AUTONOMY-DEPLOYMENT-PLAN v2 final (all 3 personas green-lit), awaiting Toby ratification
- Quinn: workshop refinement ratified 4 decisions, waiting to drop CALBEAF ticket for Booker side (ladder tie-breaker, skip_class_only narrow)
- Gotan: niche-readiness dossier delivered for SwingTiempo hypothesis

## Files Touched

**New:**
- `scripts/lib/safety.ts`
- `data/runs/incident_2026-04-17_fb-soft-block.md`
- `data/runs/dq_sweep_group_context_leak_2026-04-17.md`
- `data/runs/prod_scrub_group_context_leak_2026-04-17.csv`
- `data/rate-limit-state.json` (armed, blocks runs until 2026-04-19 ~12:57Z)
- Memory: `feedback_fb_access_conservative.md`, `reference_jira_projects.md`

**Modified:**
- `scripts/batch-pipeline.ts` (preflight + Stage 1 DOM detector + humanDelay + caps + flags)
- `scripts/watchdog.sh` (path rot fix + expanded keyword match + state-file check)

**Not committed to git yet.** Leaving for next session to decide whether to PR as separate commits (B1, B2, watchdog-fix, incident-docs) or bundled.

## UPDATE: Porter DQ Audit V1 (22:19Z post-scrub)

Porter applied my 6-row CSV (3 patched + 2 deleted) AND ran a broader DQ audit across prod. Much larger scope than my sweep — **139 total Booker-side findings**:

| Check | Severity | Count | Description |
|---|---|---|---|
| `venue_loc_consistency` | high | 45 | US country with non-US state, or foreign-country marker in venue name |
| `venue_coords_vs_country` | high | 84 | Venue coords outside declared country bbox (mis-geocode / stitched) |
| `event_title_vs_venue_city` | high | 10 | Title says one city, masteredCity is another |

**What this reveals:** my Sweep 1 (marker-based LIKE match) found 4 rows. Porter's coords-vs-country bbox check found 84. Word-boundary `", Portugal"` pattern misses venue_names that omit the country (e.g., "Hongdae" in Seoul, "BUZZ Bayside" in Tokyo — no country markers in venue_name, only state codes like KR/JP). **Coords-based detection is more complete than text-based detection.**

**Samples Porter surfaced:**
- Coords in Berlin / Seoul / Tokyo / Singapore / Italy / Slovenia — all tagged country=US (84 such rows)
- State codes KR, JP, SE, PT appearing with country=US
- "Jose Garofalo in Boston-Last tour of USA" → masteredCity=NYC (classic group-context leak in title)
- "La Vista Wed. Practica DJ Steve Correl Portland" → masteredCity=San Jose (×2 copies)
- "Milonga de Paris" → masteredCity=Tokyo (extreme case)

**Next-session action added:**
- Scope of CALBEAF ticket work is now clearly ~139 prod rows + unknown SQLite tail — much bigger than 6. Booker-side fix has proportionate urgency.
- Consider if a `venue_coords_vs_country` check should also be added to Booker Stage 3 post-geocoding — detects misgeocodes before writing.
- Porter report: `porter/data/runs/dq_audit_prod_2026-04-17/booker.md` (markdown samples) + `data/runs/dq_audit_prod_2026-04-17_22-18-55.json` (machine-readable).
- Porter will re-run DQ audit with `--compare-prev` after Booker fix ships → delta tells us if fix worked.
